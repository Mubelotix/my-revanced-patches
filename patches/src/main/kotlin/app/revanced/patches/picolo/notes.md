`SubscriptionsState`
`subscribeContainer`

`PaywallParent`
`paywallType`
`_showPaywallScreen`
`endGameToPaywall`

`isPremium`
`isOldPremium`

`isLocked` could be set to always return true

`chargeable` could be set to always be false
